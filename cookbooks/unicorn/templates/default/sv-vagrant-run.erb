#!/bin/sh
exec 2>&1
#fixed error:Could not find RubyGem unicorn
export GEM_HOME=<%= node[:languages][:ruby][:gems_dir] %>
cd /vagrant
exec <%= @binary_path %> -E <%= @env %> -c <%= @config_path %> <%= "-d" if @debug %> 2>&1
